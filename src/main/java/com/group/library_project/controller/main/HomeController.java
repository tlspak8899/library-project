package com.group.library_project.controller.main;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller // @Controller 어노테이션은 스프링부트에서 템플릿 엔진(thymeleaf,jsp 등) 과 함께 뷰 랜더링 담당 / @RestController 는 RESRful API 를 위함, JSON 형태로 반환하기 위함
public class HomeController {

/*    @RequestMapping("/")
    public String home() {
        return "main/main";
    }*/
}
